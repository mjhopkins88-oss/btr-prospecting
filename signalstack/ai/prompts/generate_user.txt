Draft {n} distinct LinkedIn outreach options for this prospect.

Hard rules:
- Use ONLY the facts in CONTEXT. Never invent metrics, history, or relationships.
- Each option must reference at least one stored signal, note, or profile field.
- Do NOT anchor any option on title, company, or location alone.
- Lead with the strongest distilled observation AND/OR a real insight.
  If no strong observation or insight exists, return only low-pressure
  networking openers and set "low_context": true on each option.
- Each option must use a DIFFERENT angle from the STRATEGIES list — a
  different framing, opening, rhythm, and ask. No minor rewrites.
- Keep messages short, sharp, peer-toned. Lower the cost of "no".

PROSPECT:
{prospect_line}

CONTEXT:
{context_json}

DISTILLED OBSERVATIONS:
{observations_json}

INSIGHTS (use these to make the message NON-GENERIC):
{insights_json}

STRATEGIES (one per option, use the angle field):
{strategies_json}

USER INSTRUCTION (optional, may be empty):
{instruction}

Return JSON ONLY. Shape: { "messages": [ ... ] }.
